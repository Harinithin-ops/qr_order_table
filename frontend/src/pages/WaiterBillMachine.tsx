/**
 * WaiterBillMachine — re-exports BillMachinePage at /waiter/bill-machine
 * so waiters can access it through the WaiterLayout without needing admin login.
 */
export { default } from './BillMachine';
